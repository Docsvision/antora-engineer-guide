[TIP]
====
Начиная с модуля {pl} версии {pl-cu5}, поддерживается два {do-what}:

ifeval::["{with-config}" == "yes"]
* Из интерфейса {of-mc} без необходимости работы с форматом `.json`, {link}
* При помощи настройки в конфигурационном файле модуля, см. подробнее ниже
endif::[]
ifeval::["{with-console}" == "yes"]
* Из интерфейса {of-mc}, минуя {cns}: см. раздел {link}
* При помощи программы {cns}, в разделе {cns-section}
endif::[]
====